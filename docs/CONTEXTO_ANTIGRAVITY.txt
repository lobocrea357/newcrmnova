// CONTEXTO PARA ANTIGRAVITY - PROBLEMA CRÍTICO DE PERMISOS Y ROLES

// Hola Antigravity, necesito tu ayuda urgente con un problema persistente y crítico en el sistema de CRM.

// === PROBLEMA PRINCIPAL: VALIDACIÓN DE PERMISOS Y ROLES ===
// Los usuarios no autorizados pueden ver tabs en el cotizador que no deberían ver.
// Ejemplo: Valeria Reinozo (rol: asesor) puede ver tabs de gestión sin tener los permisos correspondientes.

// === SÍNTOMAS ESPECÍFICOS ===
// 1. Tabs del cotizador visibles para usuarios sin permisos
// 2. Sistema de validación de permisos no funciona correctamente
// 3. UserProfileContext carga datos pero la evaluación falla
// 4. Debug logging implementado pero problema persiste

// === DEBUG LOGGING IMPLEMENTADO ===
// En UserProfileContext.js (líneas 123-145):
console.log('🔐 [UserProfileContext] Perfil cargado:', {
  email: profileData.email,
  role: profileData.role?.name,
  rolePermissionsCount: rolePermissions.length,
  rolePermissions: rolePermissions,
  userPermissionsCount: userPermissions.length,
  userPermissions: userPermissions,
  revokedPermissions: revokedPermissions
})

// En cotizador/page.js (líneas 39-56):
console.log('📊 [Cotizador] Evaluación de tabs:', {
  role: profile?.role?.name,
  hasTasasPermission,
  hasMonedasPermission,
  canManageTasas,
  canManageMonedas
})

// === ARCHIVOS CLAVE PARA REVISAR ===
// dashboard/src/contexts/UserProfileContext.js - Sistema central de permisos
// dashboard/src/app/cotizador/page.js - Evaluación de visibilidad de tabs
// dashboard/src/components/layout/Sidebar.jsx - Sistema híbrido (userConfig.js + UserProfileContext)

// === PROBLEMA DE ARQUITECTURA ===
// Actualmente tenemos un SISTEMA HÍBRIDO problemático:
// - UserProfileContext: Para lógica de negocio y helpers
// - userConfig.js: Para routing del Sidebar únicamente
// Esto crea inconsistencias y dificulta el debugging

// === SISTEMA DE AGENCIAS Y SEDES - IMPLEMENTADO PERO INCOMPLETO ===
// ✅ Backend completo: services, routes, endpoints REST
// ✅ Frontend components: AgenciasManager y SedesManager
// ✅ UI integrada: tabs en página de usuarios
// ✅ API configuration: endpoints centralizados
// ✅ Base de datos: migration SQL creada

// ❌ FALTA CRÍTICO: Sistema de asignación de usuarios a agencias/sedes
// Los components AgenciasManager y SedesManager muestran usuarios asignados PERO no tienen
// funcionalidad para asignar nuevos usuarios o gestionar las asignaciones existentes.

// === FUNCIONALIDADES FALTANTES EN AGENCIAS/SEDES ===
// 1. Modal/formulario para asignar usuarios a agencias
// 2. Modal/formulario para asignar usuarios a sedes  
// 3. Lista de usuarios disponibles para asignar
// 4. Función para remover usuarios de agencias/sedes
// 5. Función para establecer agencia primaria de un usuario
// 6. Validación de que un usuario pueda estar en múltiples agencias pero solo una sede

// === ENDPOINTS DISPONIBLES PERO NO UTILIZADOS ===
// POST /api/agencias/:id/users - Asignar usuario a agencia
// DELETE /api/agencias/:id/users/:userId - Remover usuario de agencia
// PATCH /api/agencias/:id/users/:userId/primary - Establecer agencia primaria
// POST /api/sedes/:id/users - Asignar usuario a sede
// DELETE /api/sedes/:id/users/:userId - Remover usuario de sede

// === ARCHIVOS DE AGENCIAS/SEDES QUE NECESITAN MEJORAS ===
// dashboard/src/components/agencias/AgenciasManager.jsx - Faltan controles de asignación
// dashboard/src/components/sedes/SedesManager.jsx - Faltan controles de asignación

// === PASOS PARA REPRODUCIR EL PROBLEMA ===
// 1. Login como usuario sin permisos (ej: Valeria Reinozo, rol: asesor)
// 2. Navegar a /cotizador
// 3. Observar que tabs de gestión son visibles (no deberían serlo)
// 4. Revisar DevTools Console para ver logs de debug 🔐 y 📊
// 5. Analizar discrepancia entre permisos cargados y visibilidad de tabs

// === INFORMACIÓN ADICIONAL ===
// - Base de datos: Supabase con tablas profiles, roles, permissions, role_permissions, user_permissions
// - Sistema de permisos: Basado en roles con permisos granulares
// - Framework: Next.js 13+ con React Context API
// - Backend: Express.js con endpoints REST

// === PRIORIDADES SOLICITADAS ===
// 1. **URGENTE:** Solucionar problema de validación de permisos en cotizador
// 2. **ALTA:** Implementar sistema de asignación de usuarios a agencias/sedes
// 3. **MEDIA:** Unificar sistema de permisos (eliminar híbrido userConfig.js + UserProfileContext)

// === ARCHIVOS DE DOCUMENTACIÓN CREADOS ===
// docs/ESTADO_REAL_IMPLEMENTACION.md - Estado actual del sistema
// docs/PLAN_ACCION_ACTUALIZADO.md - Plan de acción detallado
// docs/IMPLEMENTACION_FINAL_COMPLETADA.md - Resumen de implementación
// docs/CHECKLIST_IMPLEMENTACION.md - Checklist completo

// Por favor, necesito que me ayudes a resolver estos problemas críticos que afectan la seguridad y funcionalidad del sistema.
